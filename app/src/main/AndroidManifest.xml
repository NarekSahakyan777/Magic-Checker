<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chekers"
        tools:targetApi="31">
        <activity
            android:name=".PasswordReset"
            android:exported="false" />
        <activity
            android:name=".EmailVerify"
            android:exported="false" />
        <activity
            android:name=".signIn"
            android:exported="false" /> <!-- Define playGame activity -->
        <activity
            android:name=".playGame"
            android:exported="false" />
        <activity
            android:name=".logIn"
            android:exported="false" /> <!-- Define the main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Intent filter for main launcher activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>